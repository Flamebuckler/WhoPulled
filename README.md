# WhoPulled

An ESO addon that detects and announces who pulled the boss first.

## Features

- 🎯 Automatically detects who pulled the boss
- 👥 Works in groups and solo
- ⚡ Uses LibGroupCombatStats for precise detection
- 🔧 Configurable settings via addon menu or chat commands

## Usage

The addon works automatically. When someone pulls a boss, a message appears in chat:

```
[WhoPulled] @Player pulled Boss Name!
```

### Commands

- `/whopulled toggle` - Enable/disable the addon
- `/whopulled debug` - Enable/disable debug mode
